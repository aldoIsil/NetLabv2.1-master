@model PagedList.IPagedList<Model.Paciente>
@using PagedList.Mvc;

@{
    ViewBag.Title = "Historial Paciente NetLab1";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Historial Paciente NetLab 1</h2>

@*@using (Html.BeginForm("HistorialPacienteNetLab1", "Paciente", FormMethod.Get))
{*@
    <div id="dvBusquedaUsuario">
        <div class="form-group">
            <div class="col-xs-12 col-sm-12 col-md-12 col-lg-1">
                @Html.Label("Documento:", new { @class = "-label col-xs-6 col-sm-3 col-md-1 col-lg-3" })
            </div>
            <div class="col-xs-12 col-sm-12 col-md-12 col-lg-2">
                <input type="text" inputmode="numeric" id="nroDocumento"/>               
            </div>
            <div class="col-xs-12 col-sm-12 col-md-12 col-lg-1">
                @Html.Label("Codigo Muestra:", new { @class = "-label col-xs-6 col-sm-3 col-md-1 col-lg-3" })
            </div>
            <div class="col-xs-12 col-sm-12 col-md-12 col-lg-2">
                <input type="text" id="CodigoMuestra"/>
            </div>
            <div class="col-xs-12 col-sm-12 col-md-12 col-lg-1">
                <button class="btn btn-primary" id="btnBuscarHistorial" value="Buscar"> <span class="glyphicon glyphicon-search"></span> Buscar</button>
            </div>
        </div>
    </div>
@*}*@
<br />
<br />
<br />
<div id="HisNetLab1" class="table-responsive">
@if (Model != null && Model.TotalItemCount > 0) { 
    foreach (var item in Model)
    {
            @Html.Partial("_PacienteNetLabv1", (Model.Paciente)ViewBag.Paciente)
    }
}
</div>

    @*@if (Model != null && Model.TotalItemCount > 0)
    {
        @Html.Partial("_PacienteNetLabv1", Model)
    }*@
@section Scripts {
    @Scripts.Render("~/Scripts/App/Paciente.js")
    @Scripts.Render("~/Scripts/App/NetLab.js")
    @Scripts.Render("~/Scripts/jquery.alerts/jquery.alerts.js")
    @Scripts.Render("~/Scripts/App/tabladata.js")
    @Scripts.Render("~/Scripts/datatables.js")
}